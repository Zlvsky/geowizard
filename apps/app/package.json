{
  "name": "app",
  "private": true,
  "type": "module",
  "homepage": "https://zlvsky.github.io/geowizard",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "deploygh": "gh-pages -d dist"
  }
}